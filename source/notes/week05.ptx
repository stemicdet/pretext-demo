<?xml version="1.0" encoding="UTF-8"?>

<section xml:id="notes-week-05">
  <title>Week 5: Lebesgue Measure (2D) </title>

  <subsection xml:id="subsec-Additivity-of-Outer">
    <title>Additivity of Outer Measure on Borel Sets</title>
    
    <p>
        Recall that outer measure is not additive on all subsets of $\mathbf{R}$. This means that there exists disjoint subsets $A$ and $B$ of $\mathbf{R}$ such that $|A\cup B| \neq |A|+|B|$.
    </p>

    <p>
        Thus outer measure defined on all subsets of $\mathbf{R}$ is not actually a measure. However, we will show that outer measure is a bonafide measure when restricted to Borel subsets of $\mathbf{R}$.
    </p>

    <p>
        This restricted outer measure, which is called Lebesgue measure, is the generalization of the concept of the length of an interval that we have been seeking.
    </p>

    <p>
        We start our proof of the additivity of outer measure on Borel sets by showing that outer measure is additive when one of the sets is open.
    </p>

    <theorem xml:id="thm-Addivitity-outer-measure">

        <title>Additivity of Outer Measure if One of the Sets is Open</title>
        
        

        <statement>
            <p>
                Suppose $A$ and $G$ are disjoint subsets of $\mathbf{R}$ and $G$ is open. Then $|A \cup G| = |A| + |G|$.
            </p>
        </statement>
    </theorem>

    <proof>
        <p>
            If $|G| = \infty$ then both sides are equal, so we may suppose that $|G| &lt; \infty$.
        </p>

        <p>
            By subadditivity of outer measure we know that $|A \cup G| \leq |A| + |G|$. Hence we just need to prove the other direction.
        </p>

        <p>
            First suppose that $G$ is the open interval $(a,b)$ with $a &lt; b$. We may also suppose that $a,b \notin A$ because removing two points from $A$ does not change its outer measure.
        </p>

        <p>
            Recall that we defined the outer measure of a set by taking the infimum over all open covers of the set. So let $I_1, I_2, \ldots$ be a sequence of open intervals whose union contains $A \cup G$.
        </p>

        <p>
            For each $N \in \mathbf{N}$ let $J_n = I_n \cap (\infty,a)$, $K_n = I_n \cap (a,b)$, and $L_n = I_n \cap (b,\infty)$. Hence $\ell(I_n) = \ell(J_n) + \ell(K_n) + \ell(J_n)$.
        </p>

        <p>
           Since $A$ and $G$ are disjoint, $K_1, K_2, \ldots$ is a sequence of open intervals that cover $G$ and $J_1, L_1, J_2, L_2, \ldots$ is a sequence of open intervals that cover $A$. Thus
           <md>
            <mrow> \sum_{n=1}^{\infty}\ell(I_n) \amp = \sum_{n=1}^{\infty} \ell(J_n) + \ell (L_n) + \sum_{n=1}^{\infty} \ell (K_n) </mrow>
            <mrow> \amp \geq |A|+|G| </mrow>
           </md>
            
            Thus $|A\cup G| \geq |A| + |G|$ and hence $|A \cup G| = |A|+|G|$ in this special case.
        </p>

        <p>
            By Induction it follows that if  $G$ is a union of finitely many disjoint intervals that are all disjoint from $A$, then $|A \cup G| = |A| + |G|$.
        </p>

        <p>
            Finally suppose that $G$ is an arbitrary open set that is disjoint from $A$. Then $G = \cup_{n=1}^{\infty} I_n$ for some sequence of disjoint open intervals $I_1, I_2, \ldots$ each of which is disjoint from $A$.
        </p>

        <p>
            Hence for each $m \in \mathbf{N}$ we have
            <md>
                <mrow> |A \cup G| \amp \geq \left|A \cup \bigcup_{n=1}^m I_n\right| </mrow>
                <mrow> \amp = |A| + \sum_{n=1}^m \ell(I_n)</mrow>
            </md>
            
        </p>

        <p>
            Thus
            <md>
                <mrow> |A \cup G| \amp \geq |A| + \sum_{n=1}^m \ell(I_n) </mrow>
                <mrow> \amp \geq |A|+|G|</mrow>
            </md>

            Thus $|A \cup G| = |A| + |G|$ and the proof is complete.
            
        </p>





    </proof>

    <p>
        Next we show that outer measure is additive if one of the two sets is closed.
    </p>

    <theorem xml:id="thm-addivitity-when-closed">
        <title>Additivity of Outer Measure if One of the Sets is Closed</title>
        
        
        <statement>
            <p>
                Suppose $A$ and $F$ are disjoint subsets of $\mathbf{R}$ and $F$ is closed. Then 
                <md>
                    |A \cup F| \leq |A| + |F|.
                </md>
                
            </p>
        </statement>
    </theorem>
    

    <proof>
        <p>
            Suppose $I_1, I_2, \ldots$ is a sequence of open intervals whose union contains $A \cup F$ and let
            <md>
                G = \bigcup_{k=1}^{\infty} I_k
            </md>
            
        </p>

        <p>
            Then $G$ is an open set and $A \cup F \subseteq G$. Hence $A \subseteq G \setminus F$, which is open because it is an open set minus a closed set.
        </p>

        <p>
           If we write $G$ as $G = F \cup G \setminus F$, then $G$ is a disjoint union in which one set is open. Hence the previous theorem applies and $|G| = |F| + |G \setminus F|$. 
        </p>

        <p>
            Moreover, $|A| \leq |G \setminus F|$. Hence 
            <md>
                <mrow> |A|+|F| \leq |G \setminus F| + |F| \amp = |G|</mrow>
                <mrow> \amp \leq \sum_{k=1}^{\infty} \ell(I_k)</mrow>
            </md>
            
        </p>

        <p>
            Since $\displaystyle \bigcup_{k=1}^{\infty} I_k$ is a cover of $A \cup F$ it follows from taking the infimum over all such covers that 
            <md>
                |A| + |F| \leq |A \cup F|
            </md>
            
        </p>

        <p>
            Since it follows from countable subadditivity that $|A \cup F| \leq |A| + |F|$, we have that $|A| + |F| = |A \cup F|$ and the theorem is proven.
        </p>

    </proof>

     <p>
            The Borel $\sigma$-algebra is the smallest $\sigma$-algebra that contains all the open sets. In general we cannot represent Borel sets by a finite combination of countable unions and intersections; however, we can nicely approximated Borel sets by closed sets.
        </p>

        <theorem xml:id="thm-approximation-from-below">
            <title>Approximation of Borel Sets from Below by Closed Sets</title>
            
            
            <statement>
                <p>
                    Suppose $B \subseteq \mathbf{R}$ is a Borel set. Then for every $\epsilon>0$, there exists a closed set $F \subseteq B$ such that $|B \setminus F| &lt; \epsilon$.
                </p>
            </statement>
        </theorem>


        <proof>
            <p>
                Let $L = \{D \subseteq \mathbf{R}: \text{ for every } \epsilon>0  \text{ there exists a closed set } F \subseteq \mathbf{R} \text{ such that } |D \setminus F| &lt; \epsilon\}$. Note that $L$ contains all closed subsets of $\mathbf{R}$. We will show that $L$ is a $\sigma$-algebra. Since $L$ is a $\sigma$-algebra that contains all closed subsets of $\mathbf{R}$ it must also contain all the open subsets of $\mathbf{R}$ and thus contains all Borel sets.
            </p>

            <p>
                First, $\emptyset \in L$ since $\emptyset$ is a closed set.
            </p>

            <p>
                Before we prove that $L$ is closed under compliments, we will prove that $L$ is closed under countable intersections. Suppose that $D_1, D_2, \ldots $ is a sequence of elements in $L$. Let $\epsilon>0$ be arbitrary. Then for each $k \in \mathbf{N}$, there exists a closed set $F_k$ such that
                <md>
                     F_k \subseteq D_k \text{ and } |D_k \setminus F_k| &lt; \frac{\epsilon}{2^k}
                </md>
                
            </p>

            <p>
                In addition
                <md>
                   \left( \bigcap_{k=1}^{\infty} D_k \right) \setminus \left( \bigcap_{k=1}^{\infty} F_k \right) \subseteq \left( \bigcap_{k=1}^{\infty} D_k \setminus F_k \right)
                </md>
                
            </p>

            <p>
                Hence by monotonicity and subadditivity
                <md>
                    <mrow> \left| \left( \bigcap_{k=1}^{\infty} D_k \right) \setminus \left( \bigcap_{k=1}^{\infty} F_k \right) \right| \amp \leq \left| \left( \bigcap_{k=1}^{\infty} D_k \setminus F_k \right)\right|\\ </mrow>
                    <mrow> \amp \leq \sum_{k=1}^{\infty} \frac{\epsilon}{2^k} \leq \epsilon. </mrow>
                </md>
                
            </p>

            <p>
                Thus $\displaystyle \bigcap_{k=1}^{\infty} D_k \in L$. So $L$ is closed under countable intersections.
            </p>

            <p>
                Now we want to show that $L$ is closed under compliments. That is, given $D \in L$ we want to show that $\mathbf{R}\setminus D$ is also a subset of $L$. First suppose that $|D| &lt; \infty$. Let $\epsilon>0$ be arbitrary. Then there exists a closed set $F \subseteq D$ such that $|D \setminus F| &lt; \frac{\epsilon}{2}$. It follows from the definition of outer measure that there exists an open set $G$ such that $D \subseteq G$ and $|G| &lt; |D| + \frac{\epsilon}{2}$.
            </p>

            <p>
                Then $\mathbf{R} \setminus G$ is a closed subset of $\mathbf{R} \setminus D$ and 
                <md>
                     (\mathbf{R}\setminus D) \setminus (\mathbf{R}\setminus G) = G \setminus D \subseteq G \setminus F
                </md>
                
            </p>

            <p>
                Thus by subadditivity,
                <md>
                    |(\mathbf{R}\setminus D) \setminus (\mathbf{R}\setminus G)| \leq |G \setminus F|
                </md>
                
            </p>

            <p>
                Since $G = G\setminus F \cup F$ and these are disjoint sets with $F$ closed it follows from additivity of outer measure with one closed set that $|G| = |G\setminus F| + |F|$. Thus $|G\setminus F| = |G| - |F|$.
            </p>

            <p>
                Thus

                <md>
                    <mrow>  |(\mathbf{R}\setminus D) \setminus (\mathbf{R}\setminus G)| \amp \leq |G|-|F|</mrow>
                    <mrow> \amp =|G|-|D| + |D|-|F| </mrow>
                </md>
                
            </p>

            <p>
                Since $D = D\setminus F \cup F$ and these are disjoint sets with $F$ closed it follows from additivity of outer measure with one closed set that $|D| = |D\setminus F| + |F|$. Thus $|D\setminus F| = |D| - |F|$.
            </p>

            <p>
                Hence
                <md>
                    <mrow> |(\mathbf{R}\setminus D) \setminus (\mathbf{R}\setminus G)| \amp =|G|-|D| + |D|-|F| </mrow>
                    <mrow> \amp &lt; \frac{\epsilon}{2} + |D\setminus F| </mrow>
                    <mrow> \amp &lt; \frac{\epsilon}{2} + \frac{\epsilon}{2} = \epsilon. </mrow>
                </md>
                
            </p>

            <p>
                Since we have that $|(\mathbf{R}\setminus D) \setminus (\mathbf{R}\setminus G)| &lt; \epsilon$ and $R \setminus G$ is closed, this means that $\mathbf{R}\setminus D \in L$.
            </p>

            <p>
                For the case that $|D| = \infty$, let $D_k = D \cap [-k,k]$. Then $D_k \in L$ since $[-k,k]$ is a closed set and belongs to $L$ and as a $\sigma$-algebra, $L$ is closed under finite intersections. Since $|D_k| \leq 2k$ and $D_k \in L$, it follows that $\mathbf{R}\setminus D_k \in L$.
            </p>

            <p>
                Because $\displaystyle D = \bigcup_{k=1}^{\infty} D_k$ it follows from De Morgan's Laws that
                <md>
                    \mathbf{R}\setminus D = \bigcap_{k=1}^{\infty} \mathbf{R} \setminus D_k.
                </md>
                
            </p>

            <p>
                Since $L$ is closed under countable intersection, this implies that $\mathbf{R} \setminus D \in L$. Thus $L$ is closed under compliments.
            </p>

            <p>
                Finally we show that $L$ is closed under countable unions. Let $D_1, D_2, \ldots$ be a sequence of subsets in $L$. Then it follows from DeMorgan's laws that
                <md>
                    \mathbf{R}\setminus \bigcup_{k=1}^{\infty} D_k = \bigcap_{k=1}^{\infty} \mathbf{R} \setminus D_k.
                </md>
                
            </p>

            <p>
                Since $L$ is closed under countable intersection, this implies that $\displaystyle \mathbf{R} \setminus \bigcup_{k=1}^{\infty} D_k \in L$. Since $L$ is closed under compliments this means that $\displaystyle \bigcup_{k=1}^{\infty} D_k \in L$. Thus $L$ is closed under countable unions and hence $L$ is a $\sigma$-algebra that contains all Borel subsets of $\mathbf{R}$. 
            </p>

        </proof>

        <p>
            Now we can prove that the outer measure is additive provided that one of the sets is a Borel set.
        </p>

        <theorem xml:id="thm-additivity-outer-measure-Borel">
            <title>Additivity of Outer Measure if One Set is a Borel Set</title>
            
            
            <statement>
                <p>
                    Suppose that $A$ and $B$ are disjoint subsets of $\mathbf{R}$ and $B$ is a Borel set. Then $|A \cup B| = |A| + |B|$.
                </p>
            </statement>
        </theorem>
        
        <proof>
            <p>
                Let $\epsilon>0$ be arbitrary and let $F$ be a closed set such that $F \subseteq B$ and $|B \setminus F| &lt; \epsilon$. Then it follows from monotonicity and additivity of outer measure when one set is closed that
                <md>
                    <mrow> |A \cup B| \amp |A \cup F| </mrow>
                    <mrow> \amp = |A| + |F|</mrow>
                    <mrow> \amp = |A|+ |B| - |B \setminus F| </mrow>
                    <mrow> \amp \geq |A| + |B| - \epsilon</mrow>
                </md>
                 
            </p>

            <p>
                Because this inequality holds for all $\epsilon>0$ we have that $|A \cup B| \geq |A|+|B|$ and hence $|A \cup B| = |A| + |B|$.
            </p>

        </proof>

        <p>
            As a corollary, we can show that there are subsets of $\mathbf{R}$ that are not Borel sets.
        </p>

        <corollary xml:id="cor-non-Borel-set">
            <title>Existence of a Subset of $\mathbf{R}$that is Not a Borel Set</title>
            
            
            <statement>
                <p>
                    There exists a subset $B$ of $\mathbf{R}$ that is not a Borel set.
                </p>
            </statement>
        </corollary>
    

        <proof>
            <p>
                When we proved that outer measure is not additive in general, we constructed disjoint sets $A$ and $B$ with $|A| + |B| \neq |A \cup B|$. By the previous theorem this means that neither $A$ nor $B$ can be Borel sets.
            </p>
        </proof>

        <p>
            We can finally prove that outer measure restricted to Borel sets is a bonafide measure.
        </p>

        <theorem xml:id="thm-outer-measure-is-measure-borel">
            <title>Outer Measure is a Measure on Borel Sets</title>
            
            
            <statement>
                <p>
                    Outer measure is a measure on $(\mathbf{R}, B)$ where $B$ is the set of Borel subsets of $\mathbf{R}$.
                </p>
            </statement>
        </theorem>

        <proof>
            <p>
                We need to show that outer measure on Borel sets satisfies countable additivity. Let $B_1, B_2, \ldots$ be a sequence of Borel sets. Then it follows from additivity of outer measure on Borel sets that 
                <md>
                    \left| \bigcup_{k=1}^{\infty} B_k\right| \geq \left| \bigcup_{k=1}^{n} B_k\right| = \sum_{k=1}^n |B_k|.
                </md>
                
            </p>

            <p>
                Thus by taking the limit as $n \to \infty$ we have that
                <md>
                    \left| \bigcup_{k=1}^{\infty} B_k\right| \geq  \sum_{k=1}^{\infty} |B_k|.
                </md>
                
            </p>

            <p>
                Since the other direction of the inequality follows from countable subadditivity we have that 
                <md>
                    \left| \bigcup_{k=1}^{\infty} B_k\right| =  \sum_{k=1}^{\infty} |B_k|.
                </md>
                and thus outer measure on Borel sets is countably additive and hence is a measure.
            </p>

        </proof>

        <p>
            We are now in position to define Lebesgue measure.
        </p>
        
        <definition xml:id="def-lebesgue-measure">
            <title>Lebesgue Measure</title>
            
            
            <statement>
                <p>
                    <term>Lebesgue Measure</term> is the measure on $(\mathbf{R}, B)$ where $B$ is the $\sigma$-algebra of Borel subsets of $\mathbf{R}$, that assigns to each Borel set its outer measure.
                </p>
            </statement>
        </definition>

        <p>
            Lebesgue measure is the same thing as outer measure except that it is only applied to Borel sets. Unlike outer measure, Lebesgue measure is truly a measure. We have shown that outer measure restricted to Borel sets is a bonafide measure; however, as it turns out we can do a little better. Outer measure is still a measure if restricted to a larger collection of functions called Lebesgue measurable sets.
        </p>



  </subsection>

  <subsection xml:id="subsec-Lebesgue-Measurable-Sets">
    <title>Lebesgue Measurable Sets</title>

     <definition xml:id="def-lebesgue-measureable-set">
            <statement>
                <p>
                    A set $A \subseteq \mathbf{R}$ is called <term>Lebesgue measurable</term> if there exists a Borel set $B \subseteq A$ such that $|A \setminus B| =0$.
                </p>
            </statement>
        </definition>

        <p>
            Every Borel set is a Lebesgue measurable set; however, there are Lebesgue measurable sets that are not Borel sets, but it is rather difficult to come with up an example of one. However, it can be shown that the cardinality of the collection of Borel subsets of $\mathbf{R}$ is equal to the cardinality of the real numbers, but the cardinality of the Lebesgue measurable sets is equal to the cardinality of the power set of the real numbers which is strictly greater. Thus there are vastly more Lebesgue measurable sets than Borel sets.
        </p>

        <theorem xml:id="thm-lebesgue-set-equiv">
            <title>Equivalences for Being a Lebesgue Measurable Set</title>
            
            
            <statement>
                <p>
                    Suppose $A \subseteq \mathbf{R}$. Then the following are equivalent:
                    <ol marker="(a)">
                        <li>
                            <p>
                                $A$ is Lebesgue measurable.
                            </p>
                        </li>
                        <li>
                            <p>
                                For every $\epsilon>0$ there exists a closed set $F \subseteq A$ with $|A \setminus F| &lt; \epsilon$.
                            </p>
                        </li>
                        <li>
                            <p>
                                There exist closed sets $F_1, F_2, \ldots$ contained in $A$ such that $\displaystyle |A \setminus \bigcup_{k=1}^{\infty} F_k| = 0$.
                            </p>
                        </li>
                        <li>
                            <p>
                                There exists a Borel set $B \subseteq A$ such that $|A\setminus B| = 0$.
                            </p>
                        </li>
                        <li>
                            <p>
                                For every $\epsilon>0$ there exists an open set $G \supseteq A$ with $|G \setminus A| &lt; \epsilon$.
                            </p>
                        </li>
                        <li>
                            <p>
                                There exist open sets $G_1, G_2, \ldots$ containing $A$ such that $\displaystyle \left| \left(\bigcap_{k=1}^{\infty} G_k\right) \setminus A\right| = 0$
                            </p>
                        </li>
                        <li>
                            <p>
                                There exists a Borel set $B \supseteq A$ such that $|B \setminus A| = 0$.
                            </p>
                        </li>
                    </ol>
                </p>
            </statement>
        </theorem>

        <proof>
            <p>
                (d) is the definition of Lebesgue measure so clearly (a) and (d) are equivalent. Thus we can prove the Theorem by proving the following chain of implications: (b) implies (c) implies (d) implies $\ldots$ implies (g) implies (b). Since a lot of these implications are similar, we will just show one of them. We will show that (b) implies (c).
            </p>

            <p>
                Suppose that (b) is true; that is, for every $\epsilon>0$ there exists a closed set $F \subseteq A$ with $|A \setminus F| &lt; \epsilon$.
            </p>

            <p>
                Thus for each $n \in \mathbf{N}$ there exists a closed set $F_n \subseteq A$ with $|A \setminus F_n| &lt; \frac{1}{n}$. Since
                <md>
                    A \setminus \bigcup_{k=1}^{\infty} F_k \subseteq A \setminus F_n
                </md>
                for each $n \in \mathbf{N}$ it follows from subadditivity of outer measure that
                <md>
                    \left| A \setminus \bigcup_{k=1}^{\infty} F_k \right| \leq |A \setminus F_n| &lt; \frac{1}{n}.
                </md>
                
            </p>

            <p>
                Thus by letting $n \to \infty$ we obtain that
                <md>
                    \left| A \setminus \bigcup_{k=1}^{\infty} F_k \right| = 0.
                </md>
                
            </p>

            <p>
                Thus we have shown that (b) implies (c) in our chain of implications. The other implications are similarly proved. 
            </p>

        

        </proof>
        
        <p>
            Now we can show that outer measure restricted to Lebesgue measurable sets is still a measure.
        </p>

        <theorem xml:id="thm-outer-measure-is-measure-lebesgue">
            <title>Outer Measure is a Measure on Lebesgue Integrable Sets</title>
            
            
            <statement>
                <p>
                    <ol marker="(a)">
                        <li>
                            <p>
                                The set $L$ of Lebesgue measurable subsets of $\mathbf{R}$ is a $\sigma$-algebra on $\mathbf{R}$.
                            </p>
                        </li>
                        <li>
                            <p>
                                Outer measure is a measure on $(\mathbf{R},L)$.
                            </p>
                        </li>
                    </ol>
                </p>
            </statement>
        </theorem>

        <proof>
            <p>
                We already showed that $L$ is a $\sigma$-algebra so it remains to show that outer measure on $(\mathbf{R}, L)$ is a measure. To do this we must show countable additivity of disjoint sets.
            </p>

            <p>
                Let $A_1, A_2, \ldots$ be a disjoint sequence of Lebesgue measurable sets. By the definition of Lebesgue measurable measurable sets, for each $k$, there exists a Borel set $B_k \subseteq A_k$ such that $|A_k \setminus B_k| = 0$.
            </p>

            <p>
                Because outer measure on Borel sets is countably additive, we have that
                <md>
                    <mrow> \left| \bigcup_{k=1}^{\infty} A_k \right| \amp \geq \left| \bigcup_{k=1}^{\infty} B_k \right| </mrow>
                    <mrow> \amp = \sum_{k=1}^{\infty} |B_k|.</mrow>
                </md>
                
            </p>

            <p>
                Now it follows from the subadditivity of outer measure that 
                <md>
                    <mrow> |A_k| \amp = |A_k \setminus B_k \cup B_k| </mrow>
                    <mrow> \amp \leq |A_k\setminus B_k| + |B_k| </mrow>
                    <mrow> \amp = |B_k| </mrow>
                </md>
                since $|A_k \setminus B_k| = 0$.
                
            </p>

            <p>
                Since outer measure is monotone and $B_k \subseteq A_k$, $|B_k| \leq |A_k|$ it follows that $|B_k| = |A_k|$. Hence
                <md>
                    \left| \bigcup_{k=1}^{\infty} A_k \right| \geq \sum_{k=1}^n |A_k|.
                </md>
                
            </p>

            <p>
                By subadditivity of outer measure we have that
                <md>
                    \left| \bigcup_{k=1}^{\infty} A_k \right| \leq \sum_{k=1}^n |A_k|,
                </md>
                and hence 
                <md>
                    \left| \bigcup_{k=1}^{\infty} A_k \right| = \sum_{k=1}^n |A_k|.
                </md>
                
            </p>

            <p>
                Since outer measure is countably additive on Lebesgue measurable sets, outer measure restricted to Lebesgue measurable sets is a measure.
            </p>

        </proof>

        <p>
            If $A$ is a set with outer measure $0$, then $A$ must be a Lebesgue measurable set since $\emptyset$ is a Borel set and $|A \setminus \emptyset| = 0$.
        </p>

        <p>
            Thus the set of Lebesgue measurable sets is the smallest Borel algebra containing both all open subsets of $\mathbf{R}$ and all subsets of $\mathbf{R}$ with outer measure $0$.
        </p>

        <p>
            Because outer measure is not additive on all subsets of $\mathbf{R}$ there are subsets of $\mathbf{R}$ that are not Lebesgue measurable sets.
        </p>

        <definition xml:id="def-lebesgue-measure-2">
            <title>Lebesgue Measure</title>
            
            
            <statement>
                <p>
                    <term>Lebesgue measure</term> is the measure on $(\mathbf{R}, L)$ where $L$ is the $\sigma$-algebra of Lebesgue measurable subsets of $\mathbf{R}$, that assigns to each Lebesgue measurable set its outer measure. 
                </p>
            </statement>
        </definition>

        <p>
            We have two different definitions for Lebesgue measure which only differ on the domain of the measure-- is it the $\sigma$-algebra of Borel sets or Lebesgue measurable sets?
        </p>

        <p>
            Usually it doesn't matter whether you use Borel sets or Lebesgue measurable sets. I will specify whether Lebesgue measure refers to Borel sets or Lebesgue measurable sets, unless it is irrelevant.
        </p>
        
    
  </subsection>

  <subsection xml:id="subsec-Cantor">
    <title>The Cantor Set and Cantor Function</title>
    

    <p>
        We might hope that Lebesgue measurable sets would behave nicely with respect to continuous functions; in other words, if $A$ is a Lebesgue measurable set and $f: A \to \mathbf{R}$ is a continuous function then $f(A)$ would also be a Lebesgue measurable set. 
    </p>

    <p>
        Unfortunately, this is not the case as we will demonstrate by constructing a counterexample. For this we will need to use the Cantor set.
    </p>

    <p>
        Recall that the Cantor set is constructed by starting with the interval $[0,1]$ and successively removing the middle thirds from each interval.
    </p>

    <definition xml:id="def-Cantor-set">
        <title>Cantor Set</title>
        
        
        <statement>
            <p>
                The <term>Cantor Set</term> $C$ is $\displaystyle [0,1] \setminus \cup_{n=1}^{\infty} G_n$ where $G_1 = (1/3, 2/3)$ and $G_n$ for $n &gt; 1$ is the union of the middle third open intervals in the intervals of $\displaystyle [0,1] \setminus \cup_{j=1}^{n-1} G_j$.
            </p>
        </statement>
    </definition>

    <p>
       The Cantor set can also be represented using Ternary (Base 3) expansions. In Ternary, place values represent powers of 3: threes, nines, twenty-sevens, $\frac{1}{3}$, $\frac{1}{9}$, $\ldots$. 
    </p>

    <p>
        Suppose a number has Ternary expansion $0.a_1 a_2 a_3 \ldots$. where $a_n = 0$, $1$, or $2$. The value of $a_1$ tells which third of the interval $[0,1]$ the point belongs to.
    </p>

    <p>
        If $a_1 = 0$, the point is in the left third of $[0,1]$, if $a_1 = 1$, the point is in the middle third of $[0,1]$, and if $a_1 = 2$, the point is in the right third of $[0,1]$.
    </p>

    <p>
        This means that any number whose Ternary expansion has $a_1=1$ belongs to the middle third interval and is removed in the first step of constructing the Cantor set.
    </p>

    <p>
        Likewise the value of $a_2$ tells which third of the interval $[0,\frac{1}{3}]$ or $[\frac{2}{3},1]$ the point belongs to, with $a_2 = 1$ corresponding to the middle third of that interval. 
    </p>

    <p>
        Thus any number whose Ternary expansion contains a $1$ in it will be in the middle third of some interval and will be removed in the construction of the Cantor set. Thus a point is in the Cantor set if and only if its Ternary expansion only contains the digits $0$ or $2$. 
    </p>

    <theorem xml:id="thm-Cantor-set-properties">
        <title>Properties of the Cantor Set</title>
        
        
        <statement>
            <p>
                <ol marker ="(a)">
                    <li>
                        <p>
                            The Cantor set is a closed subset of $\mathbf{R}$.
                        </p>
                    </li>
                    <li>
                        <p>
                           The Cantor set has Lebesgue measure $0$. 
                        </p>
                    </li>
                    <li>
                        <p>
                            The Cantor set contains no intervals with more than one element.
                        </p>
                    </li>
                </ol>
            </p>
        </statement>
    </theorem>

    <proof>
        <p>
            (a) The compliment of the Cantor set is a countable union of open intervals, so it is an open set. Thus the Cantor set must be closed.
        </p>

        <p>
            (b) In MTH 527 we proved that the compliment of the Cantor set has length $1$, hence it has Lebesgue measure $1$ and thus the Cantor set has Lebesgue measure $0$.
        </p>

        <p>
            (c) Since an interval with at least two elements has positive measure, it follows from (b) that the Cantor set can't contain intervals with more than one element.
        </p>
    </proof>

    <p>
        We will refer to the following function several times throughout this course.
    </p>

    <definition xml:id="def-Cantor-Function">
        <title>The Cantor Function</title>
        
        
        <statement>
            <p>
               The <term>Cantor function</term> or <term>Cantor-Lebesgue Function</term> $\Lambda: [0,1] \to [0,1]$ is defined by converting base 3 representations into base 2 representations as follows.  
            </p>

            <ul>
                <li>
                    <p>
                        If $x$ belongs to the Cantor set then $\Lambda(x)$ is computed from the unique base 3 representation of $x$ consisting only of $0$'s and $2$'s by replacing all $2$'s in the expansion by $1$'s and interpreting the resulting string as a base 2 number.
                    </p>
                </li>
                <li>
                    <p>
                        If $x$ does not belong to the Cantor set, then $\Lambda(x)$ is computed by truncating after the first $1$, replacing each $2$ before the first $1$ with $1$ and interpreting the resulting string as a base 2 number.
                    </p>
                </li>
            </ul>
        </statement>
    </definition>
    

  </subsection>

  </section>